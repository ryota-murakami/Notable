{
  "name": "@notable/electron",
  "version": "1.0.0",
  "description": "Notable Electron desktop application",
  "private": true,
  "type": "module",
  "main": "build/main.js",
  "scripts": {
    "build": "node scripts/build.cjs",
    "build:electron": "node scripts/build.cjs && electron-builder",
    "dev": "node scripts/dev-with-server.cjs",
    "dev:electron-only": "ELECTRON_START_URL=http://localhost:4378 electron .",
    "start": "electron .",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "jest",
    "e2e": "cross-env ELECTRON_HEADLESS=false playwright test",
    "e2e:headless": "cross-env ELECTRON_HEADLESS=true playwright test",
    "e2e:ci": "cross-env ELECTRON_HEADLESS=true CI=true playwright test --reporter=list",
    "e2e:debug": "cross-env ELECTRON_HEADLESS=false playwright test --debug",
    "e2e:with-server": "node scripts/test-e2e-with-dev-server.cjs",
    "clean": "rm -rf dist build node_modules pnpm-lock.yaml",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "dependencies": {
    "electron-localshortcut": "^3.2.1",
    "electron-updater": "^6.3.9",
    "node-notifier": "^10.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^24.1.0",
    "@types/node-notifier": "^8.0.5",
    "cross-env": "^7.0.3",
    "electron": "^35.2.1",
    "electron-builder": "^26.0.12",
    "eslint": "^9.32.0",
    "jest": "^29.7.0",
    "typescript": "^5.8.3"
  },
  "build": {
    "appId": "com.notable.electron",
    "productName": "Notable",
    "electronVersion": "35.2.1",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "!build/**/*.map",
      "package.json"
    ],
    "mac": {
      "category": "public.app-category.productivity"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    }
  },
  "jest": {
    "preset": "../configs/jest-config"
  }
}