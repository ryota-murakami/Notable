// Simple Jest test without React Native components
describe('Math utilities', () => {
  it('should add numbers correctly', () => {
    expect(2 + 2).toBe(4)
  })

  it('should multiply numbers correctly', () => {
    expect(3 * 4).toBe(12)
  })
})
