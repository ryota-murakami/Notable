{
  "version": 2,
  "builds": [
    {
      "src": "packages/web/next.config.mjs",
      "use": "@vercel/next",
      "config": {
        "zeroConfig": false
      }
    }
  ],
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "cd packages/web && pnpm build",
  "outputDirectory": "packages/web/.next"
}
